# TODO

- [ ] Create a skill documenting snob-case naming convention (kebab+snake hybrid: `snake_element-snake_element`)
- [x] tap-dancer exec-parallel: add `-j` flag for concurrency control (e.g. `-j 1` for sequential)
